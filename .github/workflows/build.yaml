name: Build

on:
  pull_request:
    branches: [main, development, stable, feat/*]
  push:
    branches: [main, development, stable, feat/*]

jobs:
  lint:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install poetry
          poetry install
      - name: Lint with flake8
        run: |
          make lint

#  test:
    # Include the following line to run the job on a self-hosted runner

  build:
    runs-on: ubuntu-18.04
    branches: [main, development, stable]
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-python@v2
        with:
          python-version: 3.9
      - name: docker login
        run: |
          echo ${{ secrets.DOCKER_PASSWORD }} | docker login -u ${{ secrets.DOCKER_USERNAME }} --password-stdin
      - name: Build
        run: |
          docker build -t jsiona/chat-base:latest -f docker/api/Dockerfile .
            docker push jsiona/chat-base:latest
          docker build -t jsiona/migrations-base:latest -f docker/migrations/Dockerfile .
            docker push jsiona/migrations-base:latest
          docker build -t jsiona/postgres-base:latest -f docker/postgresql/Dockerfile .
            docker push jsiona/postgres-base:latest
